<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <application>
    <!-- Debug-only regression fixture. It intentionally omits
         android:supportsPictureInPicture so the app can prove the bridge
         degrades without crashing when a customer misses that required host
         configuration. It is not included in release builds. -->
    <activity
      android:name=".NoPictureInPictureActivity"
      android:exported="true"
      android:label="PiP Missing Manifest Test" />
  </application>
</manifest>
