# The customer-supported sample set, as a machine-readable manifest.
# docs/supported-samples.md is the human-readable statement of the same set and
# its support bar; scripts/check-doc-paths.sh keeps the two in step.

# Sample directory names under samples/player that are customer-supported.
SUPPORTED_SAMPLES="ads background-playback basic-playback closed-captions custom-controls drm fullscreen offline-playback picture-in-picture playback-state playlists ssai"
